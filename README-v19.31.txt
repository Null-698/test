J6Handset iOS v19.31

Native background + Recents delete treatment

- Keypad now uses only the native iOS system background; removed the custom radial gradients.
- Settings now uses the same native iOS system background as the other primary tabs; removed the custom grouped/gradient backdrop.
- Recents swipe-to-delete now explicitly uses the native destructive red tint so Delete remains readable even with the app-wide primary tint.
- No changes to call routing, CallKit, SMS transport, call history, notification icon metadata, Android, or Magisk.
